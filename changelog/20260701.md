# 2026-07-01 修改日志

---

## 15:24 ADB 逻辑全面修复

**修改总结**（3 个文件，+69/-21 行）

### AdbHelper.java（5 处修复）

1. **权限结果逐条解析** — 用 `<<<PERM_N>>>` 分隔符包裹每条 `pm grant` 输出，按分隔符拆分后逐条判断 signature/失败/成功，不再全局匹配
2. **Socket 泄漏修复** — `connect()` 加 try-catch，异常时关闭已创建的 Socket
3. **magic 校验失败丢弃坏包** — 校验失败时 `return null` 而非仅打日志继续
4. **execShell 截断预警** — 达到 500 次迭代上限时打 warning 日志，便于排查
5. **helperHeartbeat 注解** — 标注为阻塞 I/O 方法，禁止主线程调用

### AdbKeyManager.java（1 处修复）

6. **NPE 防护** — `signToken()` 和 `getAdbPublicKeyString()` 开头检查 key 是否为 null，抛出明确异常

### MainActivity.java（3 处修复）

7. **自动授权不再清密钥** — `autoAdbGrant()` 改为 `startAdbGrant(false)`，复用已信任的密钥静默执行
8. **并发授权互斥** — 加 `AtomicBoolean adbGrantInProgress`，防止重试/手动/自动同时触发；retry 回调增加 `KEY_ADB_GRANTED` 二次检查
9. **消除重复 Helper 启动** — 移除 `checkPermissions()` 中单独的 `startHelperDaemon()` 调用，统一由 `BydVehicleManager.ensureHelperRunning()` 管理
